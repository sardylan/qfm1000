FROM ubuntu:focal

ENV DEBIAN_FRONTEND=noninteractive

RUN apt update \
    && apt install -y build-essential automake autoconf libtool pkg-config cmake cmake-curses-gui git \
        qt5-default libqt5svg5-dev libqt5serialport5-dev

